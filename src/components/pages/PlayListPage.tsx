import React from "react";
import PlaylistId from "./playlistSections/PlaylistId";

const PlayListPage = () => {
  return (
    <div>
      <PlaylistId />
    </div>
  );
};

export default PlayListPage;
